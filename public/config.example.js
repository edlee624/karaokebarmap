// Copy this file to config.js (same folder) and fill in your Supabase project
// values. Both of these are PUBLIC values (safe to ship to the browser) — the
// anon key is gated by the Row Level Security policies in supabase/migrations.
// Never put the service_role / secret key here.
window.GLOWBOOK_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT-ref.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-PUBLIC-KEY',
};
